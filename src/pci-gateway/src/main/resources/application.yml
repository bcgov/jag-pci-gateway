server:
  port: 8080
pci-gateway:
  redirect-url: ENC(yourencryptedvalue)
  gateway-clients:
    -
      merchant-id: A
      hash-key: B
      gateway-hash-key: C

jasypt:
  encryptor:
    password: ${ENC_PASSWORD}
